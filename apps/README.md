# Applications

This directory contains the independently runnable product surfaces. The root
only orchestrates build, release, and developer commands; it contains no
duplicate extension or Native Host source.

Target applications:

- `chrome-extension`: WXT browser extension and Harness Workspace shell.
- `native-server`: Native Messaging host and trusted Connector runtime.
