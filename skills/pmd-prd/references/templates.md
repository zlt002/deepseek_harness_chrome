# PRD 产品需求文档模板与填写规则

这是唯一 PRD 模板源。生成和检查 PRD 时，先遵循以下写作要求，再按模板内注释填写。

## 写作要求

- 使用最少必要信息：功能性需求是主体，同一信息只写一次；描述/痛点最多简要概括一两句；目标/价值只有用户已确认且输入资料明确有依据时才具体写；流程优先用“入口→关键操作→结果”一句话，只有确有必要才画图且只保留主干。模板章节和小节全部保留，没有内容时写“无”。
- 先写清需求点，再写代码位置。主体写清用户要什么、现在怎样、要改成怎样、如何使用和如何验收。
- 证据边界：只有第四章功能改动点可综合用户输入、用户提供/选定资料和代码查询；代码只证明现状、关联影响和研发定位，目标改动仍须用户确认或正式资料明确规定。
- 其他章节和字段不从代码库或模型推断直接填充。必填内容必须向用户确认后填写；用户提供/选定资料只能作为候选依据，不能代替确认。代码字段、变量、类名和组件名只写在第四章研发定位。
- 截图只能证明当时看到的页面，不能证明完整代码或全部影响。用户决定业务选择，不决定类名、接口或数据库实现。
- 模板标为必填的内容缺失时，先在澄清阶段向用户确认；其他内容有用户确认或确凿资料依据时填写，没有时写“无”。需求基本信息的未知字段按本章规则留空。
- 最终 PRD 删除填写注释，不保留占位符、空章节、空表或无依据数字；无内容的章节、小节或表格位置用“无”表示。
- 用户只需看到结论、需要决定的问题和推荐方案，不展示内部文件名、编号或工具参数。

除第八章新增的五类验收清单外，章节、字段和顺序均沿用《PRD 产品需求文档模板（2026/04/10）》。

```markdown
# PRD: {业务需求编号} - {业务需求名称}

# 需求基本信息

<!--
必填：业务需求名称、需求编号及链接、所属系统、产品经理、预估人天。缺失时必须向用户确认，确认前不能生成最终 PRD。
选填：优先级、评审纪要、所属功能模块。用户未确认需要填写，或没有确凿输入/资料依据时，对应单元格留空。
不猜测、不编造需求编号、链接、姓名、系统、模块和估算值，也不用文字解释信息缺失。
不要在表格下增加“后续补充”“正式排期后回填”之类的解释性备注。
填写要求：
- 业务需求名称写本次业务能力或改造结果，不写项目代号或技术方案名。
- 所属系统写业务系统名，不用代码库名代替；功能模块写用户能理解的业务模块名。
- 预估人天只有用户或正式资料明确提供时才填写。
-->
| 业务需求名称 | {名称} | 需求优先级 | {优先级} |
|---|---|---|---|
| 需求编号及链接 | {编号及链接} |  |  |
| BRD评审会议纪要链接 | {BRD链接} | PRD评审会议纪要链接 | {PRD链接} |
| 所属系统 | {系统} | 所属功能模块 | {模块} |
| 产品经理 | {产品经理} | 预估人天 | {人天} |

# 修订记录

<!-- 必填：第一版完整填写全部六列，必须经用户确认；正式流程资料只能帮助准备候选内容，不能代替确认。缺失时先询问。 -->
| 版本 | 日期 | 修订人 | 审核人 | 修订说明 | 变更分类（产品/业务） |
|---|---|---|---|---|---|
| V1.0 | {日期} | {修订人} | {审核人} | {说明} | {分类} |

<!--
只收录用户在对话中反复纠正，或明确表示不理解、需要统一解释的业务词；没有术语时保留本章并写“无”，不保留空表。
不能根据代码查询自行提取术语。代码字段、变量、类名和组件名只放在对应需求点的研发定位中。定义只写业务含义和边界。
-->
# 一、术语与缩写

| 术语/缩写 | 全称 | 定义说明 |
|---|---|---|
| {术语} | {全称} | {定义} |

<!-- 本章及其子节全部保留，没有已确认内容时写“无”。 -->
# 二、背景与目标

## （一）描述/痛点

<!-- 最多用一两句话概括谁在什么场景遇到什么问题；不展开代码实现、逐条痛点或无关数据。没有内容时写“无”。 -->
{内容；没有则写“无”}

## （二）目标/价值

<!-- 只有用户已确认，并且用户提供/选定资料也明确写出目标、价值或指标时才具体展开；不能为了完整补收益、指标或统计口径。没有内容时写“无”。 -->
{内容；没有则写“无”}

<!-- 选填：涉及资金、计费、结算、监管、审计、隐私或舆情等真实业务风险时保留；普通开发和回归风险放入对应需求点，不单独成章。 -->
## （三）风险控制

{内容；没有则写“无”}

<!-- 只有流程图能明显减少理解歧义时才画图；能用“入口→关键操作→结果”一句话说明时写该句话。必须画图时只保留主干和必要分支；没有流程内容时写“无”。 -->
# 三、整体流程

## （一）业务/功能流程图

{流程或一句话说明；没有则写“无”}

# 四、功能性需求

## （一）正常业务场景

<!--
必填，是 PRD 主体；需求较大时按“功能分组 → 改动点”层级编写：
- 改动点较多时，先按同一业务功能或用户流程分组，再在每组下展开改动点；只有一个改动点，或没有必要分组时，直接写改动点。
- 每个改动点都使用一张四列表格，一行只写一个可独立开发和验收的变化。
- “类型”只写修改、新增或删除；问题修复归入修改。
- “原有实现”写当前页面、流程、规则和结果；新增项统一写“/”。
- “目标改动点”写清要变成什么、入口、流程、规则、结果和影响；目标内容须来自用户确认或正式资料，并在末尾写研发定位：仓库相对路径、具体位置、如何改及完成后的可见结果。原有实现、关联影响和研发定位可用代码查询确认。
- 字段、状态、权限、校验、交互或接口规则较多时，在四列表格后增加对应规则表或短列表，不把所有细节挤进一个单元格，也不重复表格已有内容。
-->
### 4.1 {功能分组；无需分组时直接写改动点名称}

#### 4.1.1 {改动点名称；无需分组时删除本级标题}

| 需求点 | 阐述 | 原有实现 | 目标改动点 |
|---|---|---|---|
| {**【修改/新增/删除】**} + {需求点名称} | {从产品经理视角描述这个需求点的内容，要求无歧义，结构化} | {当前实现结构化描述；新增写“/”} | {分别从两个视角结构化输出：1、从产品视角描述具体的改动点，要求具体无歧义；2、从研发视角描述改哪里（具体说清楚功能位置，如：“订单管理→某某页面→某某按钮→双击时候”）、怎么改、要实现什么，甚至可以带上研发实现的大概编码路径。} |

{按需补充本改动点涉及的字段、状态、权限、校验、交互、输入输出或接口规则表；不需要时不添加}

<!-- 条件必填：预估超过 10 人天时保留；或明确涉及超时、并发、数据量极值时，只写实际触发的类型。每项写边界条件、系统行为、用户提示或恢复方式及依据。 -->
## 边界场景

{边界条件与系统行为；没有则写“无”}

## （二）异常业务场景

<!-- 必填：只写用户确认的异常；输入资料只能帮助准备候选内容，不能代替确认。逐条写异常发生在哪个关键节点、系统如何处理、用户看到什么和如何恢复，不从代码查询自动扩写。 -->
{异常与处理方案；没有则写“无”}

# 五、角色权限

<!-- 只写用户确认的权限；输入资料只能帮助准备候选内容，不能代替确认；不从代码查询推断角色、范围或权限。没有权限内容时保留本章并写“无”，不保留空表。 -->
| 角色 | 功能/页面 | 权限范围 | 数据范围 | 备注 |
|---|---|---|---|---|
| {角色} | {功能/页面} | {权限} | {数据范围} | {备注} |

<!--
只有用户确认，并且其输入或提供/选定资料有明确要求时才填写；不因预估人天自动生成。各小节全部保留，没有相关要求时写“无”。
-->
# 六、非功能性需求

## （一）用户与业务规模
<!-- 仅填写用户已确认的规模要求或输入资料明确提供的规模数据；没有则写“无”。 -->
{内容；没有则写“无”}

## （二）性能指标要求
<!-- 仅填写用户已确认的性能要求或输入资料明确给出的指标；没有则写“无”，不保留空表。 -->
| 指标项 | 目标值 | 测量方法 | 备注 |
|---|---|---|---|
| {指标} | {目标} | {方法} | {备注} |

## （三）安全要求
<!-- 仅填写用户已确认或输入资料明确规定的安全要求；没有则写“无”。 -->
{内容；没有则写“无”}

## （四）高可用要求
<!-- 仅填写用户已确认或输入资料明确规定的高可用要求；没有则写“无”。 -->
{内容；没有则写“无”}

## （五）监控告警要求
<!-- 仅填写用户已确认或输入资料明确规定的监控要求；没有则写“无”。 -->
{内容；没有则写“无”}

<!-- 填写已确认的业务配置、功能开关或运维参数；没有时保留本章并写“无”，不保留空表。 -->
# 七、配置与开关

| 配置项名称 | 配置Key | 默认值 | 可选值 | 是否热生效 | 说明 |
|---|---|---|---|---|---|
| {名称} | {Key} | {默认值} | {可选值} | {是否热生效} | {说明} |

# 八、测试关注点

<!-- 各小节全部保留。只写用户确认的测试内容；没有内容时写“无”。 -->
## （一）影响范围分析
<!-- 写受影响的已有功能和明确回归范围。 -->
{内容；没有则写“无”}

## （二）异常场景关注点
<!-- 写需要验证的服务、数据、网络异常及恢复结果。 -->
{内容；没有则写“无”}

## （三）性能压测要求
<!-- 写压测场景、并发量、持续时间和通过标准。 -->
{内容；没有则写“无”}

## （四）数据准备要求
<!-- 写数据量、数据来源和脱敏要求。 -->
{内容；没有则写“无”}

## （五）验收清单

<!-- 五类全部保留。有验收内容时写可观察、可判断的结果；没有内容时直接写“无”，不添加空复选框。 -->
### 正常情况
- [ ] {内容}

### 异常情况
- [ ] {内容}

### 边界情况
- [ ] {内容}

### 权限情况
- [ ] {内容}

### 兼容情况
- [ ] {内容}

<!-- 只填写产品和业务需要查看的原型、竞品分析、数据报告或会议纪要；代码查询结果放研发定位，不在这里重复。没有则写“无”。 -->
# 九、参考文档

{名称与链接；没有则写“无”}
```
