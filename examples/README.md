# 演示与历史验收资料

这里保存原 `output/` 和根 `pmd-workspace/` 中已提交的样例，不参与产品运行。

- `documents/`：演示文档和 HTML。
- `data/`：样例采集结果。
- `workspaces/`：历史业务需求工作区。
- `qa/markdown-review/`：历史界面截图和验收记录。

这些资料用于演示和问题追溯，不是当前用户的数据，也不代表最新构建已通过人工验收。
