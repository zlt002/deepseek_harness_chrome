# Product bundles

Cordis composition layers that mount product-owned plugins beside the clean
DeepSeek Harness profile. Bundle patches may replace a configured upstream row,
but must not require edits inside `upstream/deepseek-harness`.
