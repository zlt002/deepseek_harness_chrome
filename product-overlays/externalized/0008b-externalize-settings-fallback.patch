diff --git a/packages/client/ui-settings-general/src/client/SettingsRoot.tsx b/packages/client/ui-settings-general/src/client/SettingsRoot.tsx
index 6e85d67..4b6046c 100644
--- a/packages/client/ui-settings-general/src/client/SettingsRoot.tsx
+++ b/packages/client/ui-settings-general/src/client/SettingsRoot.tsx
@@ -59,10 +59,10 @@ function SettingsPanel({ rows, renderSlot, activeId, onSelect, onClose }: PanelP
   useEffect(() => { closeButton.current?.focus() }, [])

   return (
-    <div className={css.overlay} role="presentation" data-testid="settings-overlay">
+    <div className={css.overlay} role="presentation">
       <div className={css.mask} aria-hidden="true" onClick={onClose} />
-      <div className={css.panel} role="dialog" aria-modal="true" aria-labelledby={titleId} data-testid="settings-panel">
-        <nav className={css.nav} data-testid="settings-nav">
+      <div className={css.panel} role="dialog" aria-modal="true" aria-labelledby={titleId}>
+        <nav className={css.nav}>
           <div className={css.navTitle} id={titleId}>{renderSlot('settings.header', {})}</div>
           <div className={css.navList}>
             {rows.map(row => (
@@ -80,14 +80,14 @@ function SettingsPanel({ rows, renderSlot, activeId, onSelect, onClose }: PanelP
           </div>
         </nav>
         <div className={css.content}>
-          <div className={css.header} data-testid="settings-header">
+          <div className={css.header}>
             <div className={css.actions}>{renderSlot('settings.action', {})}</div>
             <button ref={closeButton} type="button" className={css.close} onClick={onClose}>
               <IconCloseOutline16 size={14} />
               <span className={css.hiddenLabel}>{renderSlot('settings.close', {})}</span>
             </button>
           </div>
-          <div className={css.options} data-testid="settings-options">
+          <div className={css.options}>
             {active !== undefined && renderSlot('settings.section', { close: onClose }, { only: active })}
           </div>
         </div>
@@ -102,10 +102,8 @@ function SettingsPanel({ rows, renderSlot, activeId, onSelect, onClose }: PanelP
  * @returns the settings shell element tree.
  */
 export function DefaultSettingsPresentation(props: SettingsPresentationOwnerProps) {
-  const { wide, compact, useSections, useOnboardingSteps, useQuickActions, useSessions, renderSlot } = props
+  const { wide, useSections, useOnboardingSteps, useSessions, renderSlot } = props
   const [open, setOpen] = useState(false)
-  const [quickOpen, setQuickOpen] = useState(false)
-  const quickRoot = useRef<HTMLDivElement>(null)
   const [activeId, setActiveId] = useState<string | undefined>(undefined)
   const [completedOnboarding, setCompletedOnboarding] = useState<ReadonlySet<string>>(() => new Set())
   const close = useCallback(() => {
@@ -125,8 +123,6 @@ export function DefaultSettingsPresentation(props: SettingsPresentationOwnerProp
   const onboardingActive = useSessions(state =>
     state.phase === 'ready'
     && (state.current === undefined || state.byId[state.current]?.blank === true))
-  const currentSessionId = useSessions(state => state.current)
-  const quickActions = useQuickActions(actions => actions)
   const onboardingStep = onboardingActive
     ? onboardingSteps.find(step => !completedOnboarding.has(step.id))
     : undefined
@@ -143,70 +139,17 @@ export function DefaultSettingsPresentation(props: SettingsPresentationOwnerProp
     })
   }, [])

-  const closeQuick = useCallback(() => { setQuickOpen(false) }, [])
-  const runQuickAction = useCallback((action: typeof quickActions[number]) => {
-    if (currentSessionId !== undefined) void action.run(currentSessionId)
-    closeQuick()
-  }, [closeQuick, currentSessionId])
-  const compactQuickActions = quickActions.filter(action => action.id !== 'conversation')
-
-  useEffect(() => {
-    if (!quickOpen) return
-    const closeOutside = (event: PointerEvent): void => {
-      if (event.target instanceof Node && !quickRoot.current?.contains(event.target)) closeQuick()
-    }
-    const closeEscape = (event: KeyboardEvent): void => {
-      if (event.key === 'Escape') closeQuick()
-    }
-    document.addEventListener('pointerdown', closeOutside)
-    document.addEventListener('keydown', closeEscape)
-    return () => {
-      document.removeEventListener('pointerdown', closeOutside)
-      document.removeEventListener('keydown', closeEscape)
-    }
-  }, [closeQuick, quickOpen])
-
   return (
     <>
-      <div
-        className={clsx(css.triggerWrap, compact && css.compactWrap)}
-        ref={quickRoot}
-        onBlur={compact ? (event) => {
-          if (event.relatedTarget instanceof Node && event.currentTarget.contains(event.relatedTarget)) return
-          closeQuick()
-        } : undefined}
+      <button
+        type="button"
+        className={clsx(css.trigger, !wide && css.rail)}
+        aria-haspopup="dialog"
+        aria-expanded={open}
+        onClick={() => { setOpen(true) }}
       >
-        <button
-          type="button"
-          className={clsx(css.trigger, !wide && css.rail, compact && css.compact)}
-          aria-haspopup={compact ? 'menu' : 'dialog'}
-          aria-expanded={compact ? quickOpen : open}
-          onClick={() => {
-            if (compact) setQuickOpen(previous => !previous)
-            else setOpen(true)
-          }}
-        >
-          {renderSlot('settings.trigger', compact === true ? { wide, compact: true } : { wide })}
-        </button>
-        {compact && quickOpen && (
-          <div className={css.quickMenu} role="menu" aria-label="快捷操作">
-            <button type="button" role="menuitem" className={css.quickItem} onClick={() => {
-              closeQuick()
-              setOpen(true)
-            }}>设置</button>
-            {compactQuickActions.map(action => (
-              <button
-                key={action.id}
-                type="button"
-                role="menuitem"
-                className={css.quickItem}
-                disabled={currentSessionId === undefined}
-                onClick={() => { runQuickAction(action) }}
-              >{action.label}</button>
-            ))}
-          </div>
-        )}
-      </div>
+        {renderSlot('settings.trigger', { wide })}
+      </button>
       {open && (
         <SettingsPanel
           rows={rows}
@@ -228,7 +171,6 @@ export function DefaultSettingsPresentation(props: SettingsPresentationOwnerProp
   )
 }

-/** Default official presentation remains available when no product adapter is installed. */
 export function SettingsRoot(props: SettingsRootComponentProps) {
   const { renderSlotChain, ...presentation } = props
   return renderSlotChain('settings.presentation', presentation, {
diff --git a/packages/client/ui-settings-general/src/client/SettingsRoot.module.css b/packages/client/ui-settings-general/src/client/SettingsRoot.module.css
index aacd653..060e8d1 100644
--- a/packages/client/ui-settings-general/src/client/SettingsRoot.module.css
+++ b/packages/client/ui-settings-general/src/client/SettingsRoot.module.css
@@ -26,57 +26,6 @@
   line-height: 22px;
 }

-.triggerWrap {
-  position: relative;
-  flex: none;
-}
-
-.compactWrap {
-  display: flex;
-}
-
-.quickMenu {
-  position: absolute;
-  /* Keep the hover path continuous from the gear into the menu. A physical
-     gap would fire the wrapper's mouseleave before the pointer reaches it. */
-  top: 100%;
-  right: 0;
-  z-index: 1001;
-  display: flex;
-  flex-direction: column;
-  min-width: 132px;
-  padding: 4px;
-  border: 1px solid var(--dsw-alias-border-l1);
-  border-radius: 12px;
-  background: var(--dsw-specific-menu);
-  box-shadow: var(--dsw-shadow-lv3);
-}
-
-.quickItem {
-  width: 100%;
-  min-height: 34px;
-  padding: 6px 10px;
-  border: 0;
-  border-radius: 8px;
-  background: transparent;
-  color: var(--dsw-alias-label-primary);
-  font: inherit;
-  font-size: 13px;
-  line-height: 20px;
-  text-align: left;
-  cursor: pointer;
-}
-
-.quickItem:hover:not(:disabled),
-.quickItem:focus-visible {
-  background: var(--dsw-alias-interactive-bg-hover);
-}
-
-.quickItem:disabled {
-  color: var(--dsw-alias-label-dimmed);
-  cursor: default;
-}
-
 .trigger:hover {
   background: var(--dsw-alias-interactive-bg-hover);
 }
@@ -92,13 +41,6 @@
   border-radius: 50%;
 }

-.trigger.compact {
-  width: 34px;
-  height: 34px;
-  margin: 0;
-  border-radius: 10px;
-}
-
 .triggerLabel {
   overflow: hidden;
   white-space: nowrap;
@@ -282,86 +224,3 @@
   clip: rect(0 0 0 0);
   white-space: nowrap;
 }
-
-/* Compact side panels have the full Harness viewport. The header stays in
-   the content column while only `.options` scrolls, so the close/action row
-   remains available as sections grow beyond the viewport. */
-@media (max-width: 999px) {
-  .panel {
-    display: grid;
-    grid-template-columns: minmax(0, 1fr) auto;
-    grid-template-rows: auto minmax(0, 1fr);
-    width: 100%;
-    height: 100%;
-    max-width: none;
-    max-height: none;
-    border-radius: 0;
-  }
-
-  .nav {
-    grid-column: 1;
-    grid-row: 1;
-    width: auto;
-    min-width: 0;
-    padding: 8px 0 8px 8px;
-    overflow: hidden;
-  }
-
-  .navTitle {
-    display: none;
-  }
-
-  .navList {
-    flex-direction: row;
-    gap: 2px;
-    min-width: 0;
-    overflow-x: auto;
-  }
-
-  .navCell {
-    flex: none;
-    width: 36px;
-    padding: 8px;
-    justify-content: center;
-  }
-
-  .navCell.active {
-    width: auto;
-    justify-content: flex-start;
-  }
-
-  .navLabel {
-    display: none;
-  }
-
-  .navCell.active .navLabel {
-    display: inline;
-  }
-
-  .content {
-    display: contents;
-  }
-
-  .navCell {
-    padding-right: 8px;
-    padding-left: 8px;
-  }
-
-  .header {
-    grid-column: 2;
-    grid-row: 1;
-    position: relative;
-    z-index: 1;
-    height: 56px;
-    padding: 14px 8px;
-    background: var(--dsw-alias-bg-layer-2);
-  }
-
-  .options {
-    grid-column: 1 / -1;
-    grid-row: 2;
-    padding-right: 16px;
-    padding-bottom: 16px;
-    padding-left: 16px;
-  }
-}
diff --git a/packages/client/ui-settings-general/tests/settings-root.client.spec.tsx b/packages/client/ui-settings-general/tests/settings-root.client.spec.tsx
index f731642..0e386c3 100644
--- a/packages/client/ui-settings-general/tests/settings-root.client.spec.tsx
+++ b/packages/client/ui-settings-general/tests/settings-root.client.spec.tsx
@@ -4,7 +4,6 @@ import { useEffect, useState } from 'react'
 import { act, cleanup, fireEvent, render, screen } from '@testing-library/react'
 import type { SettingsRootComponentProps } from '../src/client/shell-contract.ts'
 import { SettingsRoot } from '../src/client/SettingsRoot.tsx'
-import { TriggerContent } from '../src/client/chrome.tsx'

 afterEach(cleanup)

@@ -21,7 +20,6 @@ const SEAT_CONTENT: Record<string, string> = {

 function mount({
   wide = true,
-  compact = false,
   onboardingActive = true,
   rows = [
     { id: 'general', order: 0, label: 'General' },
@@ -32,18 +30,7 @@ function mount({
     { id: 'welcome', order: -100 },
     { id: 'credential', order: 0 },
   ],
-  quickActions = [
-    { id: 'trajectory', label: '轨迹', order: 10, run: vi.fn() },
-    { id: 'copy-session-log', label: '复制日志', order: 20, run: vi.fn() },
-  ],
-}: {
-  wide?: boolean
-  compact?: boolean
-  onboardingActive?: boolean
-  rows?: Row[]
-  steps?: Step[]
-  quickActions?: Array<{ id: string; label: string; order: number; run: () => void }>
-} = {}) {
+}: { wide?: boolean; onboardingActive?: boolean; rows?: Row[]; steps?: Step[] } = {}) {
   // Mutable row source standing in for the bound useSections hook; bump()
   // plays a ledger change through the same observable contract.
   let current = rows
@@ -66,7 +53,6 @@ function mount({
     useSessions,
     useWorkspaces: unusedHook,
     wide,
-    compact,
     useOnboardingSteps: select => select(steps),
     useSections: (select) => {
       const [, force] = useState(0)
@@ -77,7 +63,6 @@ function mount({
       }, [])
       return select(current)
     },
-    useQuickActions: select => select(quickActions),
     renderSlot,
     renderSlotChain: ((_key, _owner, options) => options?.fallback ?? null) as never,
   }
@@ -95,13 +80,6 @@ function openPanel() {
   fireEvent.click(screen.getByRole('button', { name: 'Settings' }))
 }

-describe('Settings trigger chrome', () => {
-  it('uses the vertical-more icon in the compact header only', () => {
-    render(<TriggerContent {...({ wide: false, compact: true, t: () => 'Settings' } as Parameters<typeof TriggerContent>[0])} />)
-    expect(document.querySelector('svg')?.className.baseVal).toContain('compactMore')
-  })
-})
-
 describe('SettingsRoot trigger', () => {
   it('renders the trigger seat content as the accessible name (no aria-label of its own)', () => {
     const { renderSlot } = mount()
@@ -118,36 +96,6 @@ describe('SettingsRoot trigger', () => {
     const { renderSlot } = mount({ wide: false })
     expect(renderSlot).toHaveBeenCalledWith('settings.trigger', { wide: false })
   })
-
-  it('opens the compact menu only on click and routes actions for the current Session', () => {
-    mount({ wide: false, compact: true, onboardingActive: false })
-    const trigger = screen.getByRole('button', { name: 'Settings' })
-    fireEvent.focus(trigger)
-    expect(screen.queryByRole('menu')).toBeNull()
-    fireEvent.mouseEnter(trigger)
-    expect(screen.queryByRole('menu')).toBeNull()
-    fireEvent.click(trigger)
-    expect(screen.getByRole('menu')).toBeTruthy()
-    expect(screen.getByRole('menuitem', { name: '设置' })).toBeTruthy()
-    expect(screen.getByRole('menuitem', { name: '轨迹' })).toBeTruthy()
-    fireEvent.click(screen.getByRole('menuitem', { name: '复制日志' }))
-    expect(screen.queryByRole('menu')).toBeNull()
-  })
-
-  it('never offers both trajectory and conversation in the compact quick menu', () => {
-    mount({
-      wide: false,
-      compact: true,
-      onboardingActive: false,
-      quickActions: [
-        { id: 'trajectory', label: '轨迹', order: 10, run: vi.fn() },
-        { id: 'conversation', label: '对话', order: 11, run: vi.fn() },
-      ],
-    })
-    fireEvent.click(screen.getByRole('button', { name: 'Settings' }))
-    expect(screen.getByRole('menuitem', { name: '轨迹' })).toBeTruthy()
-    expect(screen.queryByRole('menuitem', { name: '对话' })).toBeNull()
-  })
 })

 describe('SettingsPanel chrome seats', () => {
@@ -155,10 +103,6 @@ describe('SettingsPanel chrome seats', () => {
     mount()
     openPanel()
     const dialog = screen.getByRole('dialog')
-    expect(screen.getByTestId('settings-overlay').contains(dialog)).toBe(true)
-    expect(screen.getByTestId('settings-nav')).toBeTruthy()
-    expect(screen.getByTestId('settings-header')).toBeTruthy()
-    expect(screen.getByTestId('settings-options')).toBeTruthy()
     const titleId = dialog.getAttribute('aria-labelledby')!
     expect(titleId).toBeTruthy()
     const title = document.getElementById(titleId)!
