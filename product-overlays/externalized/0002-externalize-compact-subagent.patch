diff --git b/packages/client/ui-subagent/README.i18n.yaml a/packages/client/ui-subagent/README.i18n.yaml
index 29a2a0fbb7..6ba288bf5a 100644
--- b/packages/client/ui-subagent/README.i18n.yaml
+++ a/packages/client/ui-subagent/README.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write packages/client/ui-subagent/README.md
-README.md: 9992c25c6580731c0a46452eda02a279f2fb4bbf
-README.zh.md: 73d05375ae6ff1356c4eefabcb9b63d37ab11b70
+README.md: 555278253131dcb6b65d19252fcaec5d0667d93f
+README.zh.md: 4ff62131ea47041e7daa1012282c1340af851204
diff --git b/packages/client/ui-subagent/README.md a/packages/client/ui-subagent/README.md
index 9992c25c65..5552782531 100644
--- b/packages/client/ui-subagent/README.md
+++ a/packages/client/ui-subagent/README.md
@@ -2,7 +2,7 @@
 
 English | [中文](README.zh.md)
 
-Web subagent feature owner: contributes the lazily expandable catalog tree to `conversation.session.header.actions`, a running-only compact-header entry to `sidebar.compact.action`, reason-specific read-only replacements to the conversation composer chain, and the existing `@` reference source to `ctx.inputTriggers`.
+Web subagent feature owner: contributes the lazily expandable catalog tree to `conversation.session.header.actions`, reason-specific read-only replacements to the conversation composer chain, and the existing `@` reference source to `ctx.inputTriggers`.
 
 The header action reads `subagentsByParent` and session summaries through the standard `useSessions` hook. After a non-empty direct catalog arrives, its trigger counts the complete subagent-only descendant lineage, stops at ordinary forks, and shows ongoing activity when any counted descendant is running. The compact tree remains direct-catalog authoritative: continuable and one-shot rows display mode plus `running`/`inactive` activity and an optional log-backed title, while the trailing column stacks total durable provider usage above active-turn duration. Token totals sum the four disjoint `tokenUsage` buckets. Visual duration stays exact to the second below one day, then uses at most two adjacent units—days/hours, approximate months/days, or approximate years/months—while hover and the accessible name retain the exact day/hour/minute/second value. Duration sums completed `subagentTiming` turns, advances once per second only for an open turn on a running child, and freezes after the child becomes inactive; an interrupted open turn is bounded by its same-cut `active.through`, never by newer session metadata. An unlabeled one-shot row falls back to its session id, while corrupt, unsupported, or unavailable rows remain readable but disabled. Each healthy row's `hasChildren` hint determines disclosure before interaction, so known leaves never show an arrow; a catalog level reserves the disclosure column only when at least one healthy row is a branch, allowing branchless levels to start at the leading status marker. Expanding a branch immediately reserves one disabled loading row per known direct descendant, then lazily replaces them with that child's authoritative catalog. Every visible branch is reported to the runtime so membership frames cause a debounced refresh only where the tree is being consumed. Selecting any depth calls `SessionRuntime.openSubagent()` with the row's exact `{parentSessionId, childSessionId, mode}` address. Component-local state owns tree visibility, expanded branches, keyboard focus, and the running-duration clock. ArrowRight/ArrowLeft expand and collapse branches; ArrowUp/ArrowDown, Home, End, and Escape navigate or close the tree; closing returns focus to the trigger. Styling uses tokens only.
 
@@ -10,8 +10,6 @@ A one-shot child always elects a read-only composer that identifies the transcri
 
 Subagent-origin Session rows are omitted from the ordinary sidebar, so the parent header catalog is their navigation entry point. Ordinary forks remain in the sidebar.
 
-Below the 1000px compact-shell breakpoint, the current parent Session shows a badge-only subagent entry only while its direct catalog has live `running` children. Its hover, focus, or click menu lists only those current direct children, with the existing catalog-derived navigation address and any available durable token/duration metrics. The entry is absent for another Session's children, inactive children, an unavailable current Session, and all wide or rail sidebar presentations.
-
 The `@` source remains deliberately separate and inert. Candidates are zero-RPC running children from `ctx.sessions.list`; picking one inserts literal `@label ` text, and the codec projects `@label`. It has no command-adjudication hooks and does not resolve labels into continuation addresses.
 
 ## Model Experience
diff --git b/packages/client/ui-subagent/README.zh.md a/packages/client/ui-subagent/README.zh.md
index 73d05375ae..4ff62131ea 100644
--- b/packages/client/ui-subagent/README.zh.md
+++ a/packages/client/ui-subagent/README.zh.md
@@ -2,7 +2,7 @@
 
 [English](README.md) | 中文
 
-Web subagent 功能 owner：向 `conversation.session.header.actions` 贡献可懒加载展开的目录树，向 `sidebar.compact.action` 贡献仅展示运行中子代理的紧凑页头入口，向会话编辑器链贡献按原因区分的只读替代呈现，并保留注册到 `ctx.inputTriggers` 的既有 `@` 引用 source。
+Web subagent 功能 owner：向 `conversation.session.header.actions` 贡献可懒加载展开的目录树，向会话编辑器链贡献按原因区分的只读替代呈现，并保留注册到 `ctx.inputTriggers` 的既有 `@` 引用 source。
 
 页头操作通过标准 `useSessions` 钩子读取 `subagentsByParent` 与会话摘要。非空直接目录到达后，其触发器会统计仅含 subagent 的完整后代谱系，在普通 fork 处停止，并在任一计入统计的后代处于 `running` 时显示活动仍在进行。紧凑树仍以直接目录为权威依据：可继续和 one-shot 行会显示 mode、`running`／`inactive` 活动状态和由日志支撑的可选 title，尾随列则在上行显示提供方的持久化 token 用量总计，在下行显示活跃轮次耗时。token 用量总计为四个互不重叠的 `tokenUsage` 桶之和。视觉耗时在不足一天时精确到秒，达到一天后则最多使用两个相邻单位——天／小时、近似月份／天或近似年份／月份——而悬停信息与无障碍名称会保留精确的天／小时／分钟／秒数值。耗时会累加已完成的 `subagentTiming` 轮次，仅在运行中 child 存在未结束轮次时每秒递增一次，并在 child 变为 inactive 后冻结；被中断的未结束轮次以其同一切面的 `active.through` 为上界，绝不使用更新的会话元数据。没有 label 的 one-shot 行会回退到其会话 id，而损坏、不受支持或不可用的行仍保持可读但禁用。每个健康行的 `hasChildren` 提示会在交互前决定是否显示展开控件，因此已知叶子节点从不显示箭头；每层目录仅在其中至少一个健康行是分支时才预留展开列，使完全不含分支的层级能从最前面的状态标记开始。展开分支时，会立即为每个已知直接后代预留一行禁用的加载行，随后再用该 child 的权威目录懒加载结果替换这些占位行。每个可见分支都会上报给运行时，使成员帧只在树正被消费的位置触发去抖动刷新。选择任意深度的条目都会使用该行的确切地址 `{parentSessionId, childSessionId, mode}` 调用 `SessionRuntime.openSubagent()`。组件局部状态负责树的可见性、已展开分支、键盘焦点与运行中耗时时钟。ArrowRight／ArrowLeft 展开和折叠分支；ArrowUp／ArrowDown、Home、End 与 Escape 用于导航或关闭树；关闭后焦点返回触发器。样式只使用 token。
 
@@ -10,8 +10,6 @@ one-shot child 始终选用只读编辑器，并将 transcript（文本记录）
 
 普通侧边栏会省略带 subagent origin 的会话行，因此 parent 页头目录是它们的导航入口。普通 fork 仍保留在侧边栏中。
 
-在低于 1000px 的紧凑外壳中，只有当前 parent Session 的直接目录含有仍处于 `running` 状态的 child 时，才显示带数字徽标的子代理入口。悬停、聚焦或点击后，菜单只列出这些当前直接 child，并使用既有目录派生的导航地址及已存在的持久化 token／耗时指标。其他 Session 的 child、已不活跃的 child、没有当前 Session 的情况，以及宽屏或轨道侧边栏呈现都不会显示该入口。
-
 `@` source 仍然刻意保持独立且惰性。候选是从 `ctx.sessions.list` 零 RPC 得到的运行中 child；pick 会插入字面文本 `@label `，codec 投影为 `@label`。它不参与命令裁决，也不会把 label 解析成继续执行地址。
 
 ## 模型体验
diff --git b/packages/client/ui-subagent/package.json a/packages/client/ui-subagent/package.json
index c13c95e908..d748ce54dd 100644
--- b/packages/client/ui-subagent/package.json
+++ a/packages/client/ui-subagent/package.json
@@ -35,8 +35,6 @@
         "@deepseek-ai/dsh-client-locale",
         "@deepseek-ai/dsh-client-runtime",
         "@deepseek-ai/dsh-client-ui-conversation",
-        "@deepseek-ai/dsh-client-ui-sidebar",
-        "@deepseek-ai/dsh-client-ui-settings",
         "@deepseek-ai/dsh-client-ui-primitives",
         "@deepseek-ai/dsh-client-ui-input-trigger"
       ],
@@ -55,8 +53,6 @@
     "@deepseek-ai/dsh-client-locale": "workspace:^",
     "@deepseek-ai/dsh-client-runtime": "workspace:^",
     "@deepseek-ai/dsh-client-ui-conversation": "workspace:^",
-    "@deepseek-ai/dsh-client-ui-sidebar": "workspace:^",
-    "@deepseek-ai/dsh-client-ui-settings": "workspace:^",
     "@deepseek-ai/dsh-client-ui-primitives": "workspace:^",
     "@deepseek-ai/dsh-client-ui-input-trigger": "workspace:^",
     "@deepseek-ai/dsh-client-ui-slots": "workspace:^",
@@ -70,8 +66,6 @@
     "@deepseek-ai/dsh-client-runtime": "workspace:^",
     "@deepseek-ai/dsh-client-test-runtime": "workspace:^",
     "@deepseek-ai/dsh-client-ui-conversation": "workspace:^",
-    "@deepseek-ai/dsh-client-ui-sidebar": "workspace:^",
-    "@deepseek-ai/dsh-client-ui-settings": "workspace:^",
     "@deepseek-ai/dsh-client-ui-primitives": "workspace:^",
     "@deepseek-ai/dsh-client-ui-input-trigger": "workspace:^",
     "@deepseek-ai/dsh-client-ui-slots": "workspace:^",
diff --git b/packages/client/ui-subagent/src/client/CompactSubagentAction.module.css a/packages/client/ui-subagent/src/client/CompactSubagentAction.module.css
deleted file mode 100644
index d4d554e980..0000000000
--- b/packages/client/ui-subagent/src/client/CompactSubagentAction.module.css
+++ /dev/null
@@ -1,126 +0,0 @@
-.root {
-  position: relative;
-  flex: none;
-}
-
-.trigger {
-  position: relative;
-  display: inline-flex;
-  align-items: center;
-  justify-content: center;
-  width: 34px;
-  height: 34px;
-  padding: 0;
-  border: 0;
-  border-radius: 10px;
-  background: transparent;
-  color: var(--dsw-alias-label-primary);
-  cursor: pointer;
-}
-
-.trigger:hover,
-.trigger:focus-visible,
-.trigger[aria-expanded='true'] {
-  background: var(--dsw-alias-interactive-bg-hover);
-}
-
-.badge {
-  position: absolute;
-  top: 1px;
-  right: 0;
-  display: inline-flex;
-  align-items: center;
-  justify-content: center;
-  min-width: 15px;
-  height: 15px;
-  padding: 0 3px;
-  box-sizing: border-box;
-  border: 1px solid var(--dsw-alias-bg-base);
-  border-radius: 8px;
-  background: var(--dsw-alias-state-business-primary);
-  color: var(--dsw-alias-label-on-fill);
-  font-size: 10px;
-  font-weight: 600;
-  line-height: 14px;
-  font-variant-numeric: tabular-nums;
-}
-
-.menu {
-  position: absolute;
-  top: 100%;
-  right: 0;
-  z-index: 1001;
-  display: flex;
-  flex-direction: column;
-  width: min(328px, calc(100vw - 20px));
-  max-height: min(360px, calc(100vh - 72px));
-  padding: 4px;
-  overflow-y: auto;
-  border: 1px solid var(--dsw-alias-border-l1);
-  border-radius: 12px;
-  background: var(--dsw-specific-menu);
-  box-shadow: var(--dsw-shadow-lv3);
-  --dsh-scrollbar-thumb: var(--dsw-alias-scrollbar-bg-l2);
-  --dsh-scrollbar-thumb-hover: var(--dsw-alias-scrollbar-hover-l2);
-}
-
-.item {
-  display: flex;
-  align-items: flex-start;
-  gap: 8px;
-  width: 100%;
-  min-height: 48px;
-  padding: 7px 8px;
-  border: 0;
-  border-radius: 8px;
-  background: transparent;
-  color: var(--dsw-alias-label-primary);
-  font: inherit;
-  text-align: left;
-  cursor: pointer;
-}
-
-.item:hover,
-.item:focus-visible {
-  background: var(--dsw-alias-interactive-bg-hover);
-}
-
-.item > :global([data-state]) {
-  flex: none;
-  margin-top: 4px;
-}
-
-.copy {
-  display: flex;
-  flex: 1;
-  flex-direction: column;
-  min-width: 0;
-}
-
-.label,
-.status {
-  overflow: hidden;
-  text-overflow: ellipsis;
-  white-space: nowrap;
-}
-
-.label {
-  font-size: 13px;
-  line-height: 18px;
-}
-
-.status,
-.metrics {
-  color: var(--dsw-alias-label-tertiary);
-  font-size: 11px;
-  line-height: 16px;
-}
-
-.metrics {
-  display: flex;
-  flex: none;
-  flex-direction: column;
-  font-variant-numeric: tabular-nums;
-  text-align: right;
-  white-space: nowrap;
-}
diff --git b/packages/client/ui-subagent/src/client/CompactSubagentAction.tsx a/packages/client/ui-subagent/src/client/CompactSubagentAction.tsx
deleted file mode 100644
index 6978af7ec5..0000000000
--- b/packages/client/ui-subagent/src/client/CompactSubagentAction.tsx
+++ /dev/null
@@ -1,185 +0,0 @@
-/** Direct subagents in the narrow sidebar header. */
-import { useEffect, useMemo, useRef, useState } from 'react'
-import {
-  IconAgentPresetOutline16, StateDot,
-} from '@deepseek-ai/dsh-client-ui-primitives'
-import type {
-  SessionId, SessionProjectionMap, SessionSummary, SubagentAddress,
-} from '@deepseek-ai/dsh-client-runtime/client'
-import type { PropsLocale, PropsRuntime, TranslateNS } from '@deepseek-ai/dsh-client-ui-slots'
-import type {} from '@deepseek-ai/dsh-client-ui-sidebar/client'
-import { NS } from './locales.ts'
-import css from './CompactSubagentAction.module.css'
-
-/** Navigation action supplied by the subagent plugin registration. */
-export interface CompactSubagentActionInjected {
-  /** Open the selected catalog child through its exact direct-parent address. */
-  openChild: (address: SubagentAddress) => void
-}
-
-/** Full props for the narrow sidebar's running-subagent affordance. */
-export type CompactSubagentActionProps =
-  PropsRuntime<'sidebar.compact.action'> & CompactSubagentActionInjected & PropsLocale<typeof NS>
-
-type CompactEntry = {
-  readonly id: SessionId
-  readonly label: string
-  readonly mode: SubagentAddress['mode']
-  readonly summary: SessionSummary | undefined
-  readonly running: boolean
-}
-
-/** Compact token count shared with the regular catalog. */
-function formatTokens(value: number): string {
-  const scaled = (next: number): string => next >= 100
-    ? String(Math.round(next))
-    : String(Math.round(next * 10) / 10)
-  if (value < 1_000) return String(value)
-  if (value < 1_000_000) return `${scaled(value / 1_000)}K`
-  return `${scaled(value / 1_000_000)}M`
-}
-
-/** Sum the durable provider-usage buckets when the projection is available. */
-function tokenTotal(usage: SessionProjectionMap['tokenUsage'] | undefined): number | undefined {
-  return usage === undefined
-    ? undefined
-    : usage.uncachedInputTokens + usage.outputTokens + usage.cacheReadTokens + usage.cacheWriteTokens
-}
-
-/** Active duration is safe only while the child is still reported as running. */
-function activityDuration(summary: SessionSummary | undefined, now: number): number | undefined {
-  const timing = summary?.projectionValues?.subagentTiming
-  if (timing === undefined) return undefined
-  if (timing.active === undefined) return timing.settledMs
-  return timing.settledMs + Math.max(0, now - timing.active.since)
-}
-
-/** Human-sized running duration; longer units are intentionally omitted from the compact header. */
-function formatDuration(ms: number, t: TranslateNS<typeof NS>): string {
-  const seconds = Math.floor(Math.max(0, ms) / 1_000)
-  const minutes = Math.floor(seconds / 60)
-  if (minutes === 0) return t('duration.seconds', { seconds })
-  if (minutes < 60) return t('duration.minutes', { minutes, seconds: seconds % 60 })
-  return t('duration.hours', {
-    hours: Math.floor(minutes / 60),
-    minutes: String(minutes % 60).padStart(2, '0'),
-    seconds: String(seconds % 60).padStart(2, '0'),
-  })
-}
-
-/**
- * Show every direct child recorded in the current session's catalog. The
- * catalog is the durable history source; live summaries refine a row's status
- * and metrics but must not hide completed children.
- */
-export function CompactSubagentAction({ useSessions, openChild, t }: CompactSubagentActionProps) {
-  const currentSessionId = useSessions(state => state.current)
-  const catalogs = useSessions(state => state.subagentsByParent)
-  const summaries = useSessions(state => state.byId)
-  const [open, setOpen] = useState(false)
-  const [now, setNow] = useState(() => Date.now())
-  const rootRef = useRef<HTMLDivElement>(null)
-  const entries = useMemo<readonly CompactEntry[]>(() => {
-    if (currentSessionId === undefined) return []
-    const catalog = catalogs[currentSessionId]
-    if (catalog === undefined) return []
-    return catalog.entries.flatMap((entry): CompactEntry[] => {
-      if (entry.kind !== 'child') return []
-      const summary = summaries[entry.id]
-      return [{
-        id: entry.id,
-        label: entry.label ?? summary?.displayTitle ?? entry.id,
-        mode: entry.mode,
-        summary,
-        running: entry.activity === 'running' && summary?.running === true,
-      }]
-    })
-  }, [catalogs, currentSessionId, summaries])
-
-  useEffect(() => {
-    if (entries.length > 0) return
-    setOpen(false)
-  }, [entries.length])
-
-  useEffect(() => {
-    if (!open || entries.length === 0) return
-    const timer = window.setInterval(() => { setNow(Date.now()) }, 1_000)
-    return () => { window.clearInterval(timer) }
-  }, [entries.length, open])
-
-  useEffect(() => {
-    if (!open) return
-    const closeOutside = (event: PointerEvent): void => {
-      if (event.target instanceof Node && !rootRef.current?.contains(event.target)) setOpen(false)
-    }
-    document.addEventListener('pointerdown', closeOutside)
-    const closeEscape = (event: KeyboardEvent): void => {
-      if (event.key === 'Escape') setOpen(false)
-    }
-    document.addEventListener('keydown', closeEscape)
-    return () => {
-      document.removeEventListener('pointerdown', closeOutside)
-      document.removeEventListener('keydown', closeEscape)
-    }
-  }, [open])
-
-  if (entries.length === 0 || currentSessionId === undefined) return null
-  const triggerKey = entries.length === 1 ? 'count.total.one' : 'count.total.other'
-
-  return (
-    <div
-      ref={rootRef}
-      className={css.root}
-      onBlur={(event) => {
-        if (event.relatedTarget instanceof Node && event.currentTarget.contains(event.relatedTarget)) return
-        setOpen(false)
-      }}
-    >
-      <button
-        type="button"
-        className={css.trigger}
-        aria-label={t(triggerKey, { count: entries.length })}
-        aria-haspopup="menu"
-        aria-expanded={open}
-        onClick={() => { setOpen(previous => !previous) }}
-      >
-        <IconAgentPresetOutline16 size={17} />
-        <span className={css.badge}>{entries.length}</span>
-      </button>
-      {open && (
-        <div className={css.menu} role="menu" aria-label={t('compact.menu.aria')}>
-          {entries.map(entry => {
-            const tokens = tokenTotal(entry.summary?.projectionValues?.tokenUsage)
-            const duration = activityDuration(entry.summary, now)
-            const status = entry.running ? t('activity.running') : t('activity.inactive')
-            return (
-              <button
-                key={entry.id}
-                type="button"
-                role="menuitem"
-                className={css.item}
-                aria-label={`${entry.label} ${status}`}
-                onClick={() => {
-                  openChild({ parentSessionId: currentSessionId, childSessionId: entry.id, mode: entry.mode })
-                  setOpen(false)
-                }}
-              >
-                <StateDot state={entry.running ? 'ongoing' : 'done'} />
-                <span className={css.copy}>
-                  <span className={css.label}>{entry.label}</span>
-                  <span className={css.status}>{status}</span>
-                </span>
-                {(tokens !== undefined || duration !== undefined) && (
-                  <span className={css.metrics}>
-                    {tokens !== undefined && <span>{formatTokens(tokens)} tok</span>}
-                    {duration !== undefined && <span>{formatDuration(duration, t)}</span>}
-                  </span>
-                )}
-              </button>
-            )
-          })}
-        </div>
-      )}
-    </div>
-  )
-}
diff --git b/packages/client/ui-subagent/src/client/CompactSubagentHeaderActions.module.css a/packages/client/ui-subagent/src/client/CompactSubagentHeaderActions.module.css
deleted file mode 100644
index 52949cbc81..0000000000
--- b/packages/client/ui-subagent/src/client/CompactSubagentHeaderActions.module.css
+++ /dev/null
@@ -1,44 +0,0 @@
-.root {
-  display: flex;
-  align-items: center;
-  gap: 2px;
-}
-
-.action {
-  display: inline-flex;
-  align-items: center;
-  justify-content: center;
-  width: 34px;
-  height: 34px;
-  padding: 0;
-  border: 0;
-  border-radius: 10px;
-  background: transparent;
-  color: var(--dsw-alias-label-primary);
-  cursor: pointer;
-}
-
-.returnConversation {
-  display: inline-flex;
-  align-items: center;
-  gap: 4px;
-  height: 30px;
-  padding: 0 8px;
-  border: 0;
-  border-radius: 8px;
-  background: transparent;
-  color: var(--dsw-alias-label-primary);
-  font: inherit;
-  font-size: 12px;
-  cursor: pointer;
-}
-
-.returnConversation:hover,
-.returnConversation:focus-visible {
-  background: var(--dsw-alias-interactive-bg-hover);
-}
-
-.action:hover,
-.action:focus-visible {
-  background: var(--dsw-alias-interactive-bg-hover);
-}
diff --git b/packages/client/ui-subagent/src/client/CompactSubagentHeaderActions.tsx a/packages/client/ui-subagent/src/client/CompactSubagentHeaderActions.tsx
deleted file mode 100644
index 8071b6d5f0..0000000000
--- b/packages/client/ui-subagent/src/client/CompactSubagentHeaderActions.tsx
+++ /dev/null
@@ -1,84 +0,0 @@
-/** Compact-only actions for an addressed child Session. */
-import {
-  IconBranchOutline16, IconCloseOutline16, IconCopyOutline16, IconNewChatOutline16, Tooltip,
-} from '@deepseek-ai/dsh-client-ui-primitives'
-import type { SessionId } from '@deepseek-ai/dsh-client-runtime/client'
-import type { HostObservable, InjectFace, PropsLocale, PropsRuntime } from '@deepseek-ai/dsh-client-ui-slots'
-import type { SettingsQuickAction } from '@deepseek-ai/dsh-client-ui-settings/client'
-import { NS } from './locales.ts'
-import css from './CompactSubagentHeaderActions.module.css'
-
-/** Operations supplied by the existing compact action registry and session runtime. */
-export interface CompactSubagentHeaderActionsInjected {
-  hooks: {
-    /** Reuse the registered trajectory and Session-log operations verbatim. */
-    quickActions: HostObservable<readonly SettingsQuickAction[]>
-    /** Current child view, bridged directly from the conversation chat store. */
-    view: HostObservable<string | null>
-  }
-  /** Return to the direct parent Session. */
-  close: (parentSessionId: SessionId) => void
-}
-
-/** Props for the narrow child-header action group. */
-export type CompactSubagentHeaderActionsProps =
-  PropsRuntime<'sidebar.compact.subagent.action'>
-  & InjectFace<CompactSubagentHeaderActionsInjected>
-  & PropsLocale<typeof NS>
-
-/** Compact subagent header actions; only exact registered actions are callable. */
-export function CompactSubagentHeaderActions({
-  sessionId, parentSessionId, useQuickActions, useView, close, t,
-}: CompactSubagentHeaderActionsProps) {
-  const actions = useQuickActions(value => value)
-  const view = useView(value => value)
-  const trajectory = actions.find(action => action.id === 'trajectory')
-  const conversation = actions.find(action => action.id === 'conversation')
-  const copyLog = actions.find(action => action.id === 'copy-session-log')
-  const viewAction = view === 'trajectory' ? conversation : trajectory
-  const viewLabel = view === 'trajectory' ? t('compact.detail.conversation') : t('compact.detail.trajectory')
-  const viewIcon = view === 'trajectory' ? 'conversation' : 'trajectory'
-
-  return (
-    <div className={css.root}>
-      {viewAction !== undefined && (
-        <Tooltip label={viewLabel} delayMs={500}>
-          <button
-            type="button"
-            className={css.action}
-            aria-label={viewLabel}
-            onClick={() => { void viewAction.run(sessionId) }}
-          >
-            <span data-testid={`compact-detail-${viewIcon}-icon`}>
-              {view === 'trajectory'
-                ? <IconNewChatOutline16 size={17} />
-                : <IconBranchOutline16 size={17} />}
-            </span>
-          </button>
-        </Tooltip>
-      )}
-      {copyLog !== undefined && (
-        <Tooltip label={t('compact.detail.copyLog')} delayMs={500}>
-          <button
-            type="button"
-            className={css.action}
-            aria-label={t('compact.detail.copyLog')}
-            onClick={() => { void copyLog.run(sessionId) }}
-          >
-            <IconCopyOutline16 size={17} />
-          </button>
-        </Tooltip>
-      )}
-      <Tooltip label={t('compact.detail.close')} delayMs={500}>
-        <button
-          type="button"
-          className={css.action}
-          aria-label={t('compact.detail.close')}
-          onClick={() => { close(parentSessionId) }}
-        >
-          <IconCloseOutline16 size={17} />
-        </button>
-      </Tooltip>
-    </div>
-  )
-}
diff --git b/packages/client/ui-subagent/src/client/CompactTrajectoryHeaderActions.tsx a/packages/client/ui-subagent/src/client/CompactTrajectoryHeaderActions.tsx
deleted file mode 100644
index e02313d16b..0000000000
--- b/packages/client/ui-subagent/src/client/CompactTrajectoryHeaderActions.tsx
+++ /dev/null
@@ -1,63 +0,0 @@
-/** Compact regular-session trajectory controls. */
-import {
-  IconCopyOutline16, IconNewChatOutline16, Tooltip,
-} from '@deepseek-ai/dsh-client-ui-primitives'
-import type { HostObservable, InjectFace, PropsLocale, PropsRuntime } from '@deepseek-ai/dsh-client-ui-slots'
-import type { SettingsQuickAction } from '@deepseek-ai/dsh-client-ui-settings/client'
-import { NS } from './locales.ts'
-import css from './CompactSubagentHeaderActions.module.css'
-
-/** Quick actions and current view supplied by the existing root services. */
-export interface CompactTrajectoryHeaderActionsInjected {
-  hooks: {
-    quickActions: HostObservable<readonly SettingsQuickAction[]>
-    view: HostObservable<string | null>
-  }
-}
-
-/** Props for the compact regular-session trajectory header controls. */
-export type CompactTrajectoryHeaderActionsProps =
-  PropsRuntime<'sidebar.compact.trajectory.action'>
-  & InjectFace<CompactTrajectoryHeaderActionsInjected>
-  & PropsLocale<typeof NS>
-
-/** A clear text return control plus the existing session-log copy affordance. */
-export function CompactTrajectoryHeaderActions({
-  sessionId, useQuickActions, useView, t,
-}: CompactTrajectoryHeaderActionsProps) {
-  const actions = useQuickActions(value => value)
-  const view = useView(value => value)
-  const conversation = actions.find(action => action.id === 'conversation')
-  const copyLog = actions.find(action => action.id === 'copy-session-log')
-
-  if (view !== 'trajectory') return null
-  return (
-    <div className={css.root}>
-      {conversation !== undefined && (
-        <Tooltip label={t('compact.detail.conversation')} delayMs={500}>
-          <button
-            type="button"
-            className={css.returnConversation}
-            aria-label={t('compact.detail.conversation')}
-            onClick={() => { void conversation.run(sessionId) }}
-          >
-            <IconNewChatOutline16 size={17} />
-            <span>{t('compact.detail.conversation')}</span>
-          </button>
-        </Tooltip>
-      )}
-      {copyLog !== undefined && (
-        <Tooltip label={t('compact.trajectory.copyLog')} delayMs={500}>
-          <button
-            type="button"
-            className={css.action}
-            aria-label={t('compact.trajectory.copyLog')}
-            onClick={() => { void copyLog.run(sessionId) }}
-          >
-            <IconCopyOutline16 size={17} />
-          </button>
-        </Tooltip>
-      )}
-    </div>
-  )
-}
diff --git b/packages/client/ui-subagent/src/client/index.ts a/packages/client/ui-subagent/src/client/index.ts
index 921160111a..93a86a8801 100644
--- b/packages/client/ui-subagent/src/client/index.ts
+++ a/packages/client/ui-subagent/src/client/index.ts
@@ -15,16 +15,7 @@ import type {
   ClientContext, SessionId, SubagentAddress,
 } from '@deepseek-ai/dsh-client-runtime/client'
 import type { ComposerChainProps } from '@deepseek-ai/dsh-client-ui-conversation/client'
-import type {} from '@deepseek-ai/dsh-client-ui-sidebar/client'
-import type {} from '@deepseek-ai/dsh-client-ui-settings/client'
 import type { ClientSessionContext, InputTriggerServiceContract, InputTriggerSource } from '@deepseek-ai/dsh-client-ui-input-trigger/client'
-import { CompactSubagentAction, type CompactSubagentActionInjected } from './CompactSubagentAction.tsx'
-import {
-  CompactSubagentHeaderActions, type CompactSubagentHeaderActionsInjected,
-} from './CompactSubagentHeaderActions.tsx'
-import {
-  CompactTrajectoryHeaderActions, type CompactTrajectoryHeaderActionsInjected,
-} from './CompactTrajectoryHeaderActions.tsx'
 import { SubagentCatalogAction, type SubagentCatalogInjected } from './SubagentCatalogAction.tsx'
 import {
   SubagentReadOnlyComposer, type SubagentReadOnlyMatch,
@@ -42,21 +33,12 @@ declare module '@deepseek-ai/dsh-client-ui-slots' {
 export type {
   SubagentCatalogActionProps, SubagentCatalogInjected,
 } from './SubagentCatalogAction.tsx'
-export type {
-  CompactSubagentActionProps, CompactSubagentActionInjected,
-} from './CompactSubagentAction.tsx'
-export type {
-  CompactSubagentHeaderActionsProps, CompactSubagentHeaderActionsInjected,
-} from './CompactSubagentHeaderActions.tsx'
-export type {
-  CompactTrajectoryHeaderActionsProps, CompactTrajectoryHeaderActionsInjected,
-} from './CompactTrajectoryHeaderActions.tsx'
 export type {
   SubagentReadOnlyComposerProps, SubagentReadOnlyMatch,
 } from './SubagentReadOnlyComposer.tsx'
 
 /** Required services for references, conversation slots, and session navigation. */
-export const inject = ['inputTriggers', 'sessions', 'slots', 'locale', 'settingsQuickActions', 'conversationViewState']
+export const inject = ['inputTriggers', 'sessions', 'slots', 'locale']
 
 /** Claim the composer for one-shot history or an unavailable continuation owner. */
 function selectReadOnlySubagent(owner: ComposerChainProps): SubagentReadOnlyMatch | null {
@@ -125,26 +107,6 @@ export function apply(ctx: ClientContext): void {
       sessions.setSubagentCatalogOpen(parentSessionId, open)
     },
   })
-  const compactActions = (): CompactSubagentActionInjected => ({
-    openChild(address: SubagentAddress) {
-      sessions.openSubagent(address)
-    },
-  })
-  const compactHeaderActions = (): CompactSubagentHeaderActionsInjected => ({
-    hooks: {
-      quickActions: ctx.get('settingsQuickActions')!.store,
-      view: ctx.get('conversationViewState')!.currentSource(ctx.sessions.list),
-    },
-    close(parentSessionId) {
-      sessions.open(parentSessionId)
-    },
-  })
-  const compactTrajectoryActions = (): CompactTrajectoryHeaderActionsInjected => ({
-    hooks: {
-      quickActions: ctx.get('settingsQuickActions')!.store,
-      view: ctx.get('conversationViewState')!.currentSource(ctx.sessions.list),
-    },
-  })
   ctx.slots.inject(
     'conversation.session.header.actions',
     () => ctx.slots.register({
@@ -164,34 +126,4 @@ export function apply(ctx: ClientContext): void {
       select: selectReadOnlySubagent,
     }, SubagentReadOnlyComposer),
   )
-  ctx.slots.inject(
-    'sidebar.compact.action',
-    () => ctx.slots.register({
-      name: 'sidebar.compact.action',
-      id: 'running-subagents',
-      order: 0,
-      locale: NS,
-      inject: compactActions,
-    }, CompactSubagentAction),
-  )
-  ctx.slots.inject(
-    'sidebar.compact.subagent.action',
-    () => ctx.slots.register({
-      name: 'sidebar.compact.subagent.action',
-      id: 'subagent-detail-actions',
-      order: 0,
-      locale: NS,
-      inject: compactHeaderActions,
-    }, CompactSubagentHeaderActions),
-  )
-  ctx.slots.inject(
-    'sidebar.compact.trajectory.action',
-    () => ctx.slots.register({
-      name: 'sidebar.compact.trajectory.action',
-      id: 'trajectory-detail-actions',
-      order: 0,
-      locale: NS,
-      inject: compactTrajectoryActions,
-    }, CompactTrajectoryHeaderActions),
-  )
 }
diff --git b/packages/client/ui-subagent/src/client/locales.ts a/packages/client/ui-subagent/src/client/locales.ts
index 9a55d2c1d6..b3897d7216 100644
--- b/packages/client/ui-subagent/src/client/locales.ts
+++ a/packages/client/ui-subagent/src/client/locales.ts
@@ -33,14 +33,6 @@ export const zh = {
   'count.total.other': '{count} 个子代理',
   'count.running.one': '{count} 个子代理，正在运行',
   'count.running.other': '{count} 个子代理，正在运行',
-  'compact.trigger.one': '{count} 个运行中的子代理',
-  'compact.trigger.other': '{count} 个运行中的子代理',
-  'compact.menu.aria': '子代理',
-  'compact.detail.trajectory': '查看轨迹',
-  'compact.detail.conversation': '返回对话',
-  'compact.detail.copyLog': '复制子代理日志',
-  'compact.trajectory.copyLog': '复制日志',
-  'compact.detail.close': '返回父会话',
   'tree.aria': '子代理会话',
   'readonly.oneShot.title': '一次性子代理记录',
   'readonly.title': '此子代理暂时只读',
@@ -78,14 +70,6 @@ export const en: Record<SubagentKey, string> = {
   'count.total.other': '{count} subagents',
   'count.running.one': '{count} subagent running',
   'count.running.other': '{count} subagents running',
-  'compact.trigger.one': '{count} running subagent',
-  'compact.trigger.other': '{count} running subagents',
-  'compact.menu.aria': 'Subagents',
-  'compact.detail.trajectory': 'View trajectory',
-  'compact.detail.conversation': 'Return to conversation',
-  'compact.detail.copyLog': 'Copy subagent log',
-  'compact.trajectory.copyLog': 'Copy log',
-  'compact.detail.close': 'Return to parent session',
   'tree.aria': 'Subagent sessions',
   'readonly.oneShot.title': 'One-shot subagent record',
   'readonly.title': 'This subagent is read-only for now',
diff --git b/packages/client/ui-subagent/tests/browser-plugin.client.spec.ts a/packages/client/ui-subagent/tests/browser-plugin.client.spec.ts
index 9708093528..ccd48bb7c0 100644
--- b/packages/client/ui-subagent/tests/browser-plugin.client.spec.ts
+++ a/packages/client/ui-subagent/tests/browser-plugin.client.spec.ts
@@ -15,7 +15,7 @@ import { Context } from '@deepseek-ai/cordis'
 import { stubSettingsScope } from '@deepseek-ai/dsh-client-test-runtime'
 import { describe, expect, it } from 'vitest'
 import {
-  createSnapshotStore, SlotRegistry, type ConversationSnapshot, type SessionId, type SessionListState,
+  SlotRegistry, type ConversationSnapshot, type SessionId, type SessionListState,
   type SessionSummary, type SubagentAddress,
 } from '@deepseek-ai/dsh-client-runtime/client'
 import type { ComposerChainProps } from '@deepseek-ai/dsh-client-ui-conversation/client'
@@ -25,9 +25,6 @@ import { apply as applyLocale, inject as localeInject } from '@deepseek-ai/dsh-c
 import {
   SubagentCatalogAction, type SubagentCatalogInjected,
 } from '../src/client/SubagentCatalogAction.tsx'
-import {
-  CompactSubagentHeaderActions, type CompactSubagentHeaderActionsInjected,
-} from '../src/client/CompactSubagentHeaderActions.tsx'
 import {
   SubagentReadOnlyComposer, type SubagentReadOnlyMatch,
 } from '../src/client/SubagentReadOnlyComposer.tsx'
@@ -62,9 +59,6 @@ function sessionsWith(sessions: SessionSummary[]) {
     openSubagent: (address: SubagentAddress) => {
       actionCalls.push({ method: 'openSubagent', args: [address] })
     },
-    open: (sessionId: SessionId) => {
-      actionCalls.push({ method: 'open', args: [sessionId] })
-    },
     refreshSubagents: (parentSessionId: SessionId) => {
       actionCalls.push({ method: 'refreshSubagents', args: [parentSessionId] })
       return Promise.resolve()
@@ -82,8 +76,6 @@ async function provideSlotFaces(ctx: Context): Promise<void> {
     children: {
       'conversation.session.header.actions': { kind: 'list', scope: 'session' },
       'conversation.composer': { kind: 'chain', scope: 'session' },
-      'sidebar.compact.action': { kind: 'list', scope: 'root' },
-      'sidebar.compact.subagent.action': { kind: 'list', scope: 'root' },
     },
   } as never, () => null)
 }
@@ -95,8 +87,6 @@ async function fullBench(sessions: SessionSummary[]) {
   const face = sessionsWith(sessions)
   ctx.provide('inputTriggers', { registerSource: (src: InputTriggerSource) => { captured = src; return () => {} } })
   ctx.provide('sessions', face)
-  ctx.provide('settingsQuickActions', { store: createSnapshotStore([]) } as never)
-  ctx.provide('conversationViewState', { currentSource: () => createSnapshotStore(null) } as never)
   ctx.provide('connection', { api: { settings: {} }, isLoopback: false } as never)
   // ui-theme's Appearance row binds a durable scope through these two.
   ctx.provide('remote', { $on: () => () => {} } as never)
@@ -129,15 +119,13 @@ const req = (query: string) =>
 
 describe('apply', () => {
   it('declares the services it binds', () => {
-    expect(inject).toEqual(['inputTriggers', 'sessions', 'slots', 'locale', 'settingsQuickActions', 'conversationViewState'])
+    expect(inject).toEqual(['inputTriggers', 'sessions', 'slots', 'locale'])
   })
 
   it('registers the "@" subagent source; disposal frees the name (HMR safety)', async () => {
     const ctx = new Context()
     await ctx.plugin(InputTriggerService).await()
     ctx.provide('sessions', sessionsWith(FAMILY))
-    ctx.provide('settingsQuickActions', { store: createSnapshotStore([]) } as never)
-    ctx.provide('conversationViewState', { currentSource: () => createSnapshotStore(null) } as never)
     ctx.provide('connection', { api: { settings: {} }, isLoopback: false } as never)
     // ui-theme's Appearance row binds a durable scope through these two.
     ctx.provide('remote', { $on: () => () => {} } as never)
@@ -173,15 +161,10 @@ describe('apply', () => {
     actions.openChild(address)
     actions.refresh(sid('parent'))
     actions.setCatalogOpen(sid('parent'), true)
-    const compactHeaderEntry = ctx.slots.entries('sidebar.compact.subagent.action')
-      .find(entry => entry.component === CompactSubagentHeaderActions)!
-    const compactHeader = (compactHeaderEntry.inject as unknown as () => CompactSubagentHeaderActionsInjected)()
-    compactHeader.close(sid('parent'))
     expect(face.actionCalls).toEqual([
       { method: 'openSubagent', args: [address] },
       { method: 'refreshSubagents', args: [sid('parent')] },
       { method: 'setSubagentCatalogOpen', args: [sid('parent'), true] },
-      { method: 'open', args: [sid('parent')] },
     ])
 
     const composerEntry = ctx.slots.entries('conversation.composer')
diff --git b/packages/client/ui-subagent/tests/compact-subagent-action.client.spec.tsx a/packages/client/ui-subagent/tests/compact-subagent-action.client.spec.tsx
deleted file mode 100644
index 7258a45d9e..0000000000
--- b/packages/client/ui-subagent/tests/compact-subagent-action.client.spec.tsx
+++ /dev/null
@@ -1,149 +0,0 @@
-// @vitest-environment jsdom
-import { afterEach, describe, expect, it, vi } from 'vitest'
-import { cleanup, fireEvent, render, screen } from '@testing-library/react'
-import type {
-  SessionId, SessionListState, SessionSummary, SubagentCatalogSnapshot, WorkspaceListState,
-} from '@deepseek-ai/dsh-client-runtime/client'
-import { makeTranslate } from '@deepseek-ai/dsh-client-test-runtime'
-import type { CompactSubagentActionProps } from '../src/client/CompactSubagentAction.tsx'
-import { CompactSubagentAction } from '../src/client/CompactSubagentAction.tsx'
-import { zh } from '../src/client/locales.ts'
-
-afterEach(cleanup)
-
-const sid = (value: string) => value as SessionId
-const t = makeTranslate(zh)
-const summary = (id: string, running = false): SessionSummary => ({
-  id: sid(id), displayTitle: id, running, blank: false, updatedAt: 1,
-})
-const catalog = (entries: SubagentCatalogSnapshot['entries']): SubagentCatalogSnapshot => ({
-  entries, parentAvailable: true, state: 'ready', error: null,
-})
-function useSnapshot<T>(snapshot: T) {
-  return <S,>(selector: (state: T) => S): S => selector(snapshot)
-}
-function state(
-  current: SessionId | undefined,
-  summaries: readonly SessionSummary[],
-  subagentsByParent: SessionListState['subagentsByParent'],
-): SessionListState {
-  return {
-    ids: summaries.map(item => item.id),
-    byId: Object.fromEntries(summaries.map(item => [item.id, item])),
-    current,
-    phase: 'ready',
-    subagentsByParent,
-    jobsBySession: {},
-    currentAddress: undefined,
-  }
-}
-const workspaces: WorkspaceListState = {
-  items: [], archivedSessionIds: [], state: 'idle', phase: 'ready', error: null,
-  baselinesReady: true, recentWorkspaceId: undefined,
-}
-
-function props(snapshot: SessionListState, openChild = vi.fn()): CompactSubagentActionProps {
-  return {
-    useSessions: useSnapshot(snapshot),
-    useWorkspaces: useSnapshot(workspaces),
-    openChild,
-    t,
-  }
-}
-
-describe('CompactSubagentAction', () => {
-  it('keeps the compact entry for a completed child recorded in the current session history', () => {
-    const parent = sid('parent')
-    const completed = sid('completed')
-    const snapshot = state(parent, [summary('parent'), summary('completed')], {
-      [parent]: catalog([
-        { kind: 'child', id: completed, label: 'Completed research', mode: 'continuable', activity: 'inactive', hasChildren: false },
-      ]),
-    })
-    render(<CompactSubagentAction {...props(snapshot)} />)
-
-    const trigger = screen.getByRole('button', { name: '1 个子代理' })
-    expect(trigger.textContent).toContain('1')
-    fireEvent.click(trigger)
-    expect(screen.getByRole('menuitem', { name: /Completed research/ })).toBeTruthy()
-  })
-
-  it('counts all direct children of the current session and opens their existing catalog address', () => {
-    const parent = sid('parent')
-    const child = sid('worker')
-    const second = sid('reviewer')
-    const inactive = sid('finished')
-    const foreign = sid('foreign')
-    const openChild = vi.fn()
-    const snapshot = state(parent, [
-      summary('parent'), summary('worker', true), summary('reviewer', true), summary('finished'), summary('foreign', true),
-    ], {
-      [parent]: catalog([
-        { kind: 'child', id: child, label: 'Research', mode: 'continuable', activity: 'running', hasChildren: false },
-        { kind: 'child', id: second, label: 'Review', mode: 'one-shot', activity: 'running', hasChildren: false },
-        { kind: 'child', id: inactive, label: 'Finished', mode: 'one-shot', activity: 'inactive', hasChildren: false },
-      ]),
-      [foreign]: catalog([
-        { kind: 'child', id: sid('foreign-child'), label: 'Other session', mode: 'one-shot', activity: 'running', hasChildren: false },
-      ]),
-    })
-    render(<CompactSubagentAction {...props(snapshot, openChild)} />)
-
-    const trigger = screen.getByRole('button', { name: '3 个子代理' })
-    expect(trigger.textContent).toContain('3')
-    fireEvent.click(trigger)
-    expect(screen.getByRole('menu', { name: '子代理' })).toBeTruthy()
-    expect(screen.getByRole('menuitem', { name: /Research 正在运行/ })).toBeTruthy()
-    expect(screen.getByRole('menuitem', { name: /Review 正在运行/ })).toBeTruthy()
-    expect(screen.getByRole('menuitem', { name: /Finished 当前未运行/ })).toBeTruthy()
-    expect(screen.queryByRole('menuitem', { name: /Other session/ })).toBeNull()
-
-    fireEvent.click(screen.getByRole('menuitem', { name: /Research 正在运行/ }))
-    expect(openChild).toHaveBeenCalledWith({
-      parentSessionId: parent, childSessionId: child, mode: 'continuable',
-    })
-  })
-
-  it('uses only the current session catalog, even when its child is no longer running', () => {
-    const parent = sid('parent')
-    const other = sid('other')
-    const child = sid('worker')
-    const snapshot = state(parent, [summary('parent'), summary('worker')], {
-      [parent]: catalog([
-        { kind: 'child', id: child, label: 'Stale', mode: 'one-shot', activity: 'running', hasChildren: false },
-      ]),
-      [other]: catalog([
-        { kind: 'child', id: sid('other-worker'), label: 'Other', mode: 'one-shot', activity: 'running', hasChildren: false },
-      ]),
-    })
-    const view = render(<CompactSubagentAction {...props(snapshot)} />)
-    expect(screen.getByRole('button', { name: '1 个子代理' })).toBeTruthy()
-
-    const activeOther = state(other, [summary('other'), summary('other-worker', true)], snapshot.subagentsByParent)
-    view.rerender(<CompactSubagentAction {...props(activeOther)} />)
-    expect(screen.getByRole('button', { name: '1 个子代理' })).toBeTruthy()
-  })
-
-  it('opens only from click and closes on Escape or an outside pointer', () => {
-    const parent = sid('parent')
-    const snapshot = state(parent, [summary('parent'), summary('worker', true)], {
-      [parent]: catalog([
-        { kind: 'child', id: sid('worker'), label: 'Worker', mode: 'one-shot', activity: 'running', hasChildren: false },
-      ]),
-    })
-    render(<CompactSubagentAction {...props(snapshot)} />)
-    const trigger = screen.getByRole('button', { name: '1 个子代理' })
-    fireEvent.focus(trigger)
-    expect(screen.queryByRole('menu')).toBeNull()
-    fireEvent.mouseEnter(trigger)
-    expect(screen.queryByRole('menu')).toBeNull()
-    fireEvent.click(trigger)
-    expect(screen.getByRole('menu')).toBeTruthy()
-    fireEvent.keyDown(trigger, { key: 'Escape' })
-    expect(screen.queryByRole('menu')).toBeNull()
-    fireEvent.click(trigger)
-    expect(screen.getByRole('menu')).toBeTruthy()
-    fireEvent.pointerDown(document.body)
-    expect(screen.queryByRole('menu')).toBeNull()
-  })
-})
diff --git b/packages/client/ui-subagent/tests/compact-subagent-header-actions.client.spec.tsx a/packages/client/ui-subagent/tests/compact-subagent-header-actions.client.spec.tsx
deleted file mode 100644
index 856064431e..0000000000
--- b/packages/client/ui-subagent/tests/compact-subagent-header-actions.client.spec.tsx
+++ /dev/null
@@ -1,132 +0,0 @@
-// @vitest-environment jsdom
-import { afterEach, describe, expect, it, vi } from 'vitest'
-import { cleanup, fireEvent, render, screen } from '@testing-library/react'
-import { useMemo, useState } from 'react'
-import type { SessionId } from '@deepseek-ai/dsh-client-runtime/client'
-import type { SettingsQuickAction } from '@deepseek-ai/dsh-client-ui-settings/client'
-import { SettingsQuickActions } from '../../ui-settings/src/client/quick-actions.ts'
-import { makeTranslate } from '@deepseek-ai/dsh-client-test-runtime'
-import {
-  CompactSubagentHeaderActions, type CompactSubagentHeaderActionsProps,
-} from '../src/client/CompactSubagentHeaderActions.tsx'
-import {
-  CompactTrajectoryHeaderActions, type CompactTrajectoryHeaderActionsProps,
-} from '../src/client/CompactTrajectoryHeaderActions.tsx'
-import { zh } from '../src/client/locales.ts'
-
-afterEach(cleanup)
-
-const CHILD = 'child' as SessionId
-const PARENT = 'parent' as SessionId
-const t = makeTranslate(zh)
-
-/** Real compact-action registry plus the conversation view state it controls. */
-function ViewHarness() {
-  const [view, setView] = useState<'chat' | 'trajectory'>('chat')
-  const quickActions = useMemo(() => {
-    const registry = new SettingsQuickActions()
-    registry.register({
-      id: 'trajectory', label: '轨迹', order: 10,
-      run: sessionId => { if (sessionId === CHILD) setView('trajectory') },
-    })
-    registry.register({
-      id: 'conversation', label: '对话', order: 11,
-      run: sessionId => { if (sessionId === CHILD) setView('chat') },
-    })
-    return registry
-  }, [])
-  const translate = ((key: string) => key === 'compact.detail.conversation'
-    ? '返回对话'
-    : t(key as never)) as CompactSubagentHeaderActionsProps['t']
-
-  return (
-    <>
-      <output data-testid="child-view">{view}</output>
-      <CompactSubagentHeaderActions
-        {...props(quickActions.store.getSnapshot())}
-        useQuickActions={selector => selector(quickActions.store.getSnapshot())}
-        useView={selector => selector(view) as never}
-        t={translate}
-      />
-    </>
-  )
-}
-
-function props(actions: readonly SettingsQuickAction[], close = vi.fn()): CompactSubagentHeaderActionsProps {
-  return {
-    sessionId: CHILD,
-    parentSessionId: PARENT,
-    useQuickActions: selector => selector(actions),
-    useView: selector => selector(null),
-    close,
-    t,
-  } as CompactSubagentHeaderActionsProps
-}
-
-describe('CompactSubagentHeaderActions', () => {
-  it('runs the existing child-scoped trajectory and raw-log actions, then returns to its direct parent', () => {
-    const trajectory = vi.fn()
-    const copyLog = vi.fn()
-    const close = vi.fn()
-    render(<CompactSubagentHeaderActions {...props([
-      { id: 'trajectory', label: '轨迹', order: 10, run: trajectory },
-      { id: 'copy-session-log', label: '复制日志', order: 20, run: copyLog },
-    ], close)} />)
-
-    fireEvent.click(screen.getByRole('button', { name: '查看轨迹' }))
-    fireEvent.click(screen.getByRole('button', { name: '复制子代理日志' }))
-    fireEvent.click(screen.getByRole('button', { name: '返回父会话' }))
-
-    expect(trajectory).toHaveBeenCalledWith(CHILD)
-    // The registered controller's copy(sessionId) sends includeDescendants=false;
-    // passing the child here is the UI boundary that keeps parent logs out.
-    expect(copyLog).toHaveBeenCalledWith(CHILD)
-    expect(close).toHaveBeenCalledWith(PARENT)
-  })
-
-  it('never invents a missing registry action, but preserves the parent-return control', () => {
-    render(<CompactSubagentHeaderActions {...props([])} />)
-    expect(screen.queryByRole('button', { name: '查看轨迹' })).toBeNull()
-    expect(screen.queryByRole('button', { name: '复制子代理日志' })).toBeNull()
-    expect(screen.getByRole('button', { name: '返回父会话' })).toBeTruthy()
-  })
-
-  it('switches a child trajectory header back to the conversation view through registered quick actions', () => {
-    render(<ViewHarness />)
-    expect(screen.getByTestId('compact-detail-trajectory-icon')).toBeTruthy()
-    expect(screen.queryByTestId('compact-detail-conversation-icon')).toBeNull()
-    fireEvent.click(screen.getByRole('button', { name: '查看轨迹' }))
-    expect(screen.getByTestId('child-view').textContent).toBe('trajectory')
-
-    // The trajectory action is now stale: the child header must expose the
-    // opposite action, whose registered run() restores this exact view state.
-    expect(screen.getByTestId('compact-detail-conversation-icon')).toBeTruthy()
-    expect(screen.queryByTestId('compact-detail-trajectory-icon')).toBeNull()
-    fireEvent.click(screen.getByRole('button', { name: '返回对话' }))
-    expect(screen.getByTestId('child-view').textContent).toBe('chat')
-    expect(screen.getByTestId('compact-detail-trajectory-icon')).toBeTruthy()
-  })
-
-  it('gives a regular trajectory header a visible return-to-conversation control and session-log copy', () => {
-    const conversation = vi.fn()
-    const copyLog = vi.fn()
-    render(
-      <CompactTrajectoryHeaderActions
-        {...{
-          sessionId: CHILD,
-          useQuickActions: (selector: (actions: readonly SettingsQuickAction[]) => unknown) => selector([
-            { id: 'conversation', label: '对话', order: 11, run: conversation },
-            { id: 'copy-session-log', label: '复制日志', order: 20, run: copyLog },
-          ]),
-          useView: (selector: (view: string | null) => unknown) => selector('trajectory'),
-          t,
-        } as CompactTrajectoryHeaderActionsProps}
-      />,
-    )
-    expect(screen.getByRole('button', { name: '复制日志' })).toBeTruthy()
-    fireEvent.click(screen.getByRole('button', { name: '返回对话' }))
-    fireEvent.click(screen.getByRole('button', { name: '复制日志' }))
-    expect(conversation).toHaveBeenCalledWith(CHILD)
-    expect(copyLog).toHaveBeenCalledWith(CHILD)
-  })
-})
diff --git b/packages/client/ui-subagent/tsconfig.json a/packages/client/ui-subagent/tsconfig.json
index 0b648bee30..16bc05eb38 100644
--- b/packages/client/ui-subagent/tsconfig.json
+++ a/packages/client/ui-subagent/tsconfig.json
@@ -20,12 +20,6 @@
     {
       "path": "../ui-conversation"
     },
-    {
-      "path": "../ui-sidebar"
-    },
-    {
-      "path": "../ui-settings"
-    },
     {
       "path": "../ui-primitives"
     },
