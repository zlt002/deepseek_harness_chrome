diff --git a/packages/client/ui-agent-preset/src/client/AgentPresetSeat.module.css b/packages/client/ui-agent-preset/src/client/AgentPresetSeat.module.css
index a00953477e..55fe22e81b 100644
--- a/packages/client/ui-agent-preset/src/client/AgentPresetSeat.module.css
+++ b/packages/client/ui-agent-preset/src/client/AgentPresetSeat.module.css
@@ -4,10 +4,10 @@
 .seat {
   display: inline-flex;
   align-items: center;
-  justify-content: center;
-  width: 28px;
-  height: 28px;
-  padding: 0;
+  gap: 4px;
+  max-width: min(100%, 240px);
+  min-height: 28px;
+  padding: 0 8px;
   border: none;
   border-radius: 16px;
   background: transparent;
@@ -33,21 +33,7 @@
 
 .seatIcon {
   flex: none;
-  color: var(--dsw-alias-state-business-primary);
-}
-
-/* Built-ins get stable, semantic hues; authored presets stay blue rather
-   than assigning an unstable color from their mutable display names. */
-.seat[data-preset='minimal'] .seatIcon {
-  color: var(--dsw-alias-state-success-primary);
-}
-
-.seat[data-preset='cordis'] .seatIcon {
-  color: var(--dsw-alias-state-warn-primary);
-}
-
-.seat[data-preset='code'] .seatIcon {
-  color: var(--dsw-static-deepseek-600);
+  color: var(--dsw-alias-label-primary);
 }
 
 /* Introduce cue: the icon eases in on an overshoot-free expo curve (duration
@@ -105,50 +91,9 @@
   }
 }
 
-.visuallyHidden {
-  position: absolute;
-  width: 1px;
-  height: 1px;
-  padding: 0;
-  margin: -1px;
-  overflow: hidden;
-  clip: rect(0 0 0 0);
-  white-space: nowrap;
-  border: 0;
-}
-
-.panel {
-  position: absolute;
-  bottom: calc(100% + 8px);
-  left: 0;
-  z-index: 100;
-  display: grid;
-  box-sizing: border-box;
-  width: 100%;
-  grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
-  gap: 6px;
-  padding: 8px;
-  border: 1px solid var(--dsw-alias-border-l1);
-  border-radius: 14px;
-  background: var(--dsw-specific-menu);
-  box-shadow: var(--dsw-shadow-lv3);
-}
-
-.panelOption {
-  min-width: 0;
-  padding: 8px 10px;
-  border: 0;
-  border-radius: 9px;
-  background: transparent;
-  color: var(--dsw-alias-label-secondary);
-  text-align: left;
-  cursor: pointer;
-}
-
-.panelOption:hover,
-.panelOptionSelected {
-  background: var(--dsw-alias-interactive-bg-hover);
-  color: var(--dsw-alias-label-primary);
+.chevron {
+  flex: none;
+  color: var(--dsw-alias-label-caption);
 }
 
 /* Menu rows carry the name over its description: the id alone never said what
diff --git a/packages/client/ui-agent-preset/src/client/AgentPresetSeat.tsx b/packages/client/ui-agent-preset/src/client/AgentPresetSeat.tsx
index dacef11dd1..fee155524f 100644
--- a/packages/client/ui-agent-preset/src/client/AgentPresetSeat.tsx
+++ b/packages/client/ui-agent-preset/src/client/AgentPresetSeat.tsx
@@ -12,16 +12,34 @@
  * Picking stages; the choice reaches a session when one becomes current.
  */
 
-import { useEffect, useRef, useState } from 'react'
+import { useEffect, useState } from 'react'
+import type { ReactNode } from 'react'
 import type { SnapshotStore } from '@deepseek-ai/dsh-client-runtime/client'
-import type { InjectFace, PropsLocale, PropsRuntime } from '@deepseek-ai/dsh-client-ui-slots'
-import { IconAgentPresetOutline16, Menu, Tooltip } from '@deepseek-ai/dsh-client-ui-primitives'
+import type { InjectFace, PropsLocale, PropsRenderSlots, PropsRuntime } from '@deepseek-ai/dsh-client-ui-slots'
+import { IconAgentPresetOutline16, IconChevronDownOutline14, Menu } from '@deepseek-ai/dsh-client-ui-primitives'
 // Type-only: pulls the ui-conversation SlotMap merge (the hero seat).
-import { useComposerOverlay } from '@deepseek-ai/dsh-client-ui-primitives'
+import type {} from '@deepseek-ai/dsh-client-ui-conversation/client'
 import type { AgentPresetSeatState } from './seat-store.ts'
 import { presetDisplayText } from './locales.ts'
 import css from './AgentPresetSeat.module.css'
 
+export interface AgentPresetPresentationOption { id: string; name: string; description: string }
+/** Presentation-only currency; loading/staging/session adoption stays with AgentPresetSeat. */
+export interface AgentPresetPresentationOwnerProps {
+  state: AgentPresetSeatState
+  options: readonly AgentPresetPresentationOption[]
+  label: string
+  shownLabel: ReactNode
+  introducing: boolean
+  load: () => Promise<void>
+  select: (id: string) => Promise<void>
+  introduced: () => void
+  t: AgentPresetSeatProps['t']
+}
+declare module '@deepseek-ai/dsh-client-ui-slots' {
+  interface SlotMap { 'agent-preset.presentation': { kind: 'chain'; scope: 'root'; owner: AgentPresetPresentationOwnerProps } }
+}
+
 /** Registration-side business face for the hero chip. */
 export interface AgentPresetSeatInjected {
   hooks: {
@@ -60,17 +78,41 @@ function introStaggerMs(count: number): number {
 /** Full component props. */
 export type AgentPresetSeatProps =
   PropsRuntime<'conversation.hero.agentPreset'>
+  & PropsRenderSlots<'agent-preset.presentation'>
   & PropsLocale<'settings.agentPreset'>
   & InjectFace<AgentPresetSeatInjected>
 
+/** The upstream presentation remains the exact fallback when no product renderer is registered. */
+function OfficialAgentPresetPresentation({ owner }: { owner: AgentPresetPresentationOwnerProps }) {
+  const [open, setOpen] = useState(false)
+  return <Menu
+    open={open}
+    onClose={() => { setOpen(false) }}
+    items={owner.options.map(option => ({
+      id: option.id,
+      label: <span className={css.item}><span className={css.itemName}>{option.name}</span><span className={css.itemDesc}>{option.description}</span></span>,
+    }))}
+    selectedId={owner.state.current}
+    onSelect={(id) => { setOpen(false); void owner.select(id) }}
+    align="start"
+    portal
+    anchor={(
+      <button type="button" className={css.seat} aria-haspopup="menu" aria-expanded={open}
+        title={owner.state.error ?? owner.t('seatHint')} disabled={owner.state.busy} onClick={() => { setOpen(value => !value) }}>
+        <IconAgentPresetOutline16 className={owner.introducing ? `${css.seatIcon} ${css.introIcon}` : css.seatIcon} />
+        {owner.shownLabel}<IconChevronDownOutline14 className={css.chevron} />
+      </button>
+    )}
+  />
+}
+
 /**
  * Render the new-session agent-preset chip.
  * @param props - composed slot props.
  * @returns the chip, or null when the deployment composes no presets.
  */
-export function AgentPresetSeat({ load, select, introduced, useAgentPresetSeat, t }: AgentPresetSeatProps) {
+export function AgentPresetSeat({ load, select, introduced, useAgentPresetSeat, renderSlotChain, t }: AgentPresetSeatProps) {
   const state = useAgentPresetSeat(snapshot => snapshot)
-  const [open, setOpen] = useState(false)
 
   useEffect(() => {
     void load()
@@ -80,35 +122,6 @@ export function AgentPresetSeat({ load, select, introduced, useAgentPresetSeat,
   const chosenText = chosen === undefined ? undefined : presetDisplayText(chosen, t)
   const label = chosenText?.name ?? state.current
   const ready = state.options.length > 0 && state.current !== ''
-  const closeHosted = useRef<() => void>(() => {})
-  const hostedPanel = (
-    <div className={css.panel} role="menu" aria-label={label}>
-      {state.options.map((option) => {
-        const text = presetDisplayText(option, t)
-        const selected = option.id === state.current
-        return (
-          <button
-            key={option.id}
-            type="button"
-            role="menuitemradio"
-            aria-checked={selected}
-            className={selected ? `${css.panelOption} ${css.panelOptionSelected}` : css.panelOption}
-            onClick={() => {
-              closeHosted.current()
-              void select(option.id)
-            }}
-          >
-            <span className={css.item}>
-              <span className={css.itemName}>{text.name}</span>
-              <span className={css.itemDesc}>{text.description ?? t('noDescription')}</span>
-            </span>
-          </button>
-        )
-      })}
-    </div>
-  )
-  const hosted = useComposerOverlay('agent-preset', hostedPanel)
-  closeHosted.current = hosted.close
 
   // The introduce cue: the pick was staged from another screen (the settings
   // creator entry), so the chip announces it — the icon eases in and each
@@ -154,55 +167,13 @@ export function AgentPresetSeat({ load, select, introduced, useAgentPresetSeat,
     )
     : label
 
-  const trigger = (
-    <Tooltip label={state.error ?? label} side="top" delayMs={500}>
-      <button
-        type="button"
-        className={css.seat}
-        data-preset={state.current}
-        {...hosted.available ? { 'data-composer-overlay-trigger': true } : {}}
-        aria-label={state.error ?? label}
-        aria-haspopup="menu"
-        aria-expanded={hosted.available ? hosted.open : open}
-        disabled={state.busy}
-        onClick={() => { hosted.available ? hosted.toggle() : setOpen(value => !value) }}
-      >
-        <IconAgentPresetOutline16 className={introducing ? `${css.seatIcon} ${css.introIcon}` : css.seatIcon} />
-        {/* Keep the selected name for assistive tech and the existing
-            introduce lifecycle, but never spend visual composer space on it. */}
-        <span className={css.visuallyHidden}>{shownLabel}</span>
-      </button>
-    </Tooltip>
-  )
-
-  if (hosted.available) return trigger
-
-  return (
-    <Menu
-      open={open}
-      onClose={() => { setOpen(false) }}
-      items={state.options.map((option) => {
-        const text = presetDisplayText(option, t)
-        return {
-          id: option.id,
-          // Name and description together: the id alone never says what a
-          // preset does, which is why the roster carries display copy.
-          label: (
-            <span className={css.item}>
-              <span className={css.itemName}>{text.name}</span>
-              <span className={css.itemDesc}>{text.description ?? t('noDescription')}</span>
-            </span>
-          ),
-        }
-      })}
-      selectedId={state.current}
-      onSelect={(id) => {
-        setOpen(false)
-        void select(id)
-      }}
-      align="start"
-      portal
-      anchor={trigger}
-    />
-  )
+  const owner: AgentPresetPresentationOwnerProps = {
+    state,
+    options: state.options.map(option => {
+      const text = presetDisplayText(option, t)
+      return { id: option.id, name: text.name, description: text.description ?? t('noDescription') }
+    }),
+    label, shownLabel, introducing, load, select, introduced, t,
+  }
+  return <>{renderSlotChain('agent-preset.presentation', owner, { fallback: <OfficialAgentPresetPresentation owner={owner} /> })}</>
 }
diff --git a/packages/client/ui-agent-preset/src/client/index.ts b/packages/client/ui-agent-preset/src/client/index.ts
index c368f6c522..bf7cd6b53d 100644
--- a/packages/client/ui-agent-preset/src/client/index.ts
+++ b/packages/client/ui-agent-preset/src/client/index.ts
@@ -36,7 +36,7 @@ import { AGENT_PRESET_SETTINGS_NS, AgentPresetSettingsController } from './setti
 
 export type { AgentPresetLabelInjected, AgentPresetLabelProps } from './AgentPresetLabel.tsx'
 export type { AgentPresetRowInjected, AgentPresetRowProps } from './AgentPresetRow.tsx'
-export type { AgentPresetSeatInjected, AgentPresetSeatProps } from './AgentPresetSeat.tsx'
+export type { AgentPresetPresentationOption, AgentPresetPresentationOwnerProps, AgentPresetSeatInjected, AgentPresetSeatProps } from './AgentPresetSeat.tsx'
 export type { AgentPresetSectionInjected, AgentPresetSectionProps } from './AgentPresetSection.tsx'
 export type { AgentPresetSeatState, SeatSessionSummary } from './seat-store.ts'
 export {
@@ -163,6 +163,7 @@ export function apply(ctx: ClientContext): void {
       }
       const chip = scope.slots.register({
         name: 'conversation.hero.agentPreset',
+        children: { 'agent-preset.presentation': { kind: 'chain', scope: 'root' } },
         locale: 'settings.agentPreset',
         inject: seatInjected,
       }, AgentPresetSeat)
diff --git a/packages/client/ui-agent-preset/tests/components.client.spec.tsx b/packages/client/ui-agent-preset/tests/components.client.spec.tsx
index c424fded0c..e82cb0696f 100644
--- a/packages/client/ui-agent-preset/tests/components.client.spec.tsx
+++ b/packages/client/ui-agent-preset/tests/components.client.spec.tsx
@@ -61,6 +61,9 @@ function renderSeat(state: Partial<AgentPresetSeatState> = {}) {
     load: vi.fn(() => Promise.resolve()),
     select: vi.fn(() => Promise.resolve()),
     introduced: vi.fn(),
+    // Unit tests exercise the official presentation path: a product may
+    // register this chain, but an unextended Harness must keep its Menu.
+    renderSlotChain: vi.fn((_name, _owner, opts) => opts?.fallback ?? null),
   }
   render(<AgentPresetSeat {...({
     ...actions,
@@ -201,11 +204,24 @@ describe('the General-settings row', () => {
 })
 
 describe('the new-session chip', () => {
-  it('reads the roster once and exposes the staged preset through the icon tooltip', async () => {
+  it('reads the roster once and shows the staged preset by name', async () => {
     const actions = renderSeat()
 
     await waitFor(() => { expect(actions.load).toHaveBeenCalledTimes(1) })
-    expect(screen.getByRole('button').getAttribute('aria-label')).toBe(en.presetStandardName)
+    expect(screen.getByRole('button').textContent).toContain(en.presetStandardName)
+    expect(screen.getByRole('button').getAttribute('title')).toBe(en.seatHint)
+  })
+
+  it('offers the presentation seam while keeping the official menu as its fallback', () => {
+    const actions = renderSeat()
+
+    expect(actions.renderSlotChain).toHaveBeenCalledWith(
+      'agent-preset.presentation',
+      expect.objectContaining({ label: en.presetStandardName, select: actions.select }),
+      expect.objectContaining({ fallback: expect.anything() }),
+    )
+    // The default fixture returns the fallback, so stock Harness behavior
+    // remains intact without any product presentation registration.
     expect(screen.getByRole('button').textContent).toContain(en.presetStandardName)
   })
 
@@ -251,10 +267,10 @@ describe('the new-session chip', () => {
     expect(screen.getByRole('button')).toHaveProperty('disabled', true)
   })
 
-  it('exposes a refused switch through the trigger label', () => {
+  it('shows a refused switch on the trigger', () => {
     renderSeat({ error: 'session has already started' })
 
-    expect(screen.getByRole('button').getAttribute('aria-label')).toBe('session has already started')
+    expect(screen.getByRole('button').getAttribute('title')).toBe('session has already started')
   })
 
   it('renders nothing before the roster arrives or when there is none', () => {
